# This file would handle any additional CRUD views not handled by the blueprint factory
# Currently empty since the CRUD operations are handled by the blueprint factory
from . import contacts_bp

# Additional routes can be registered here if needed